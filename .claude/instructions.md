# CLAUDE.md

This file provides guidance to Claude Code when working with code in this repository.

## Project Overview

This is a Node.js project using LangChain and LangGraph for building LLM applications.

## Rules

- Use npm as the package manager
- Default LLM model: deepseek-chat (unless user specifies otherwise)
- Respond in English by default (unless user specifies otherwise)
- Generate code that follows existing patterns in the codebase